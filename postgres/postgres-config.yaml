apiVersion: v1
kind: ConfigMap
metadata:
    name: postgres-config
    labels:
        app: postgres
# TODO: this should later be changed & put in a Secrets object
data:
    POSTGRES_DB: db
    POSTGRES_USER: admin
    POSTGRES_PASSWORD: admin
